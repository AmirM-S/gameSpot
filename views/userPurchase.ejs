<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Document</title>
    <link rel="stylesheet" href="/userPurchase.css" />
  </head>
  <body>
    <h1>Your Purchases</h1>
    <% if (purchases.length === 0) { %>
    <p>You have not purchased anything yet.</p>
    <% } else { %> <% purchases.forEach(purchase => { %>
    <p>
      <a href="/products/<%=purchase.product._id %>"
        ><%= purchase.product.title %></a
      >

      <span class="quantity-box"><%= purchase.quantity %></span>
    </p>
    <% }) %> <% } %>
  </body>
</html>
